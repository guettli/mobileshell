<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Process {{.Process.PID}} - MobileShell</title>
    <link href="{{.BasePath}}/static/static/bootstrap.min.css" rel="stylesheet">
    <script src="{{.BasePath}}/static/static/htmx.min.js"></script>
</head>

<body>
    <nav class="navbar navbar-dark bg-dark">
        <div class="container-fluid">
            <a href="{{.BasePath}}/" class="navbar-brand mb-0 h1">MobileShell</a>
            <div>
                <a href="{{.BasePath}}/" class="btn btn-outline-light btn-sm me-2">Workspaces</a>
                <a href="{{.BasePath}}/sysmon" class="btn btn-light btn-sm me-2">System Monitor</a>
                <a href="{{.BasePath}}/server-log" class="btn btn-outline-light btn-sm me-2">Server Log</a>
                <a href="{{.BasePath}}/logout" class="btn btn-outline-light btn-sm">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="mb-3">
            <a href="{{.BasePath}}/sysmon" class="btn btn-sm btn-outline-secondary">← Back to Process List</a>
        </div>

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Process Details: {{.Process.PID}}</h5>

                {{if eq .Process.Status "Z"}}
                <div class="alert alert-warning">
                    <strong>Zombie Process:</strong> This process has exited but is waiting for its parent to reap it.
                    Signals cannot affect zombie processes.
                </div>
                {{end}}

                <div class="row mt-3">
                    <div class="col-md-6">
                        <h6>Basic Information</h6>
                        <table class="table table-sm">
                            <tr>
                                <th style="width: 40%;">PID</th>
                                <td>{{.Process.PID}}</td>
                            </tr>
                            <tr>
                                <th>Name</th>
                                <td>{{.Process.Name}}</td>
                            </tr>
                            <tr>
                                <th>Command</th>
                                <td><code>{{.Process.Cmdline}}</code></td>
                            </tr>
                            <tr>
                                <th>Username</th>
                                <td>{{.Process.Username}}</td>
                            </tr>
                            <tr>
                                <th>Start Time</th>
                                <td>{{.Process.CreateTime.Format "2006-01-02 15:04:05 UTC"}}</td>
                            </tr>
                            <tr>
                                <th>Status</th>
                                <td>
                                    {{if eq .Process.Status "S"}}<span class="badge bg-success">Sleeping</span>{{end}}
                                    {{if eq .Process.Status "R"}}<span class="badge bg-primary">Running</span>{{end}}
                                    {{if eq .Process.Status "Z"}}<span class="badge bg-warning">Zombie</span>{{end}}
                                    {{if eq .Process.Status "T"}}<span class="badge bg-secondary">Stopped</span>{{end}}
                                    {{if eq .Process.Status "D"}}<span class="badge bg-danger">Uninterruptible</span>{{end}}
                                    {{if and (ne .Process.Status "S") (ne .Process.Status "R") (ne .Process.Status "Z") (ne .Process.Status "T") (ne .Process.Status "D")}}{{.Process.Status}}{{end}}
                                </td>
                            </tr>
                        </table>
                    </div>
                    <div class="col-md-6">
                        <h6>Resource Usage</h6>
                        <table class="table table-sm">
                            <tr>
                                <th style="width: 40%;">CPU%</th>
                                <td>{{printf "%.2f" .Process.CPUPercent}}%</td>
                            </tr>
                            <tr>
                                <th>CPU Time (User)</th>
                                <td>{{printf "%.2f" .Process.CPUTimesUser}}s</td>
                            </tr>
                            <tr>
                                <th>CPU Time (System)</th>
                                <td>{{printf "%.2f" .Process.CPUTimesSystem}}s</td>
                            </tr>
                            <tr>
                                <th>Memory (RSS)</th>
                                <td>{{printf "%.2f" .Process.MemoryMB}} MB</td>
                            </tr>
                            <tr>
                                <th>Memory %</th>
                                <td>{{printf "%.2f" .Process.MemoryPercent}}%</td>
                            </tr>
                            <tr>
                                <th>Threads</th>
                                <td>{{.Process.NumThreads}}</td>
                            </tr>
                        </table>
                    </div>
                </div>

                <div class="row mt-3">
                    <div class="col-md-6">
                        <h6>I/O Statistics</h6>
                        <table class="table table-sm">
                            <tr>
                                <th style="width: 40%;">Read</th>
                                <td>{{printf "%.2f" .Process.IOReadMB}} MB</td>
                            </tr>
                            <tr>
                                <th>Write</th>
                                <td>{{printf "%.2f" .Process.IOWriteMB}} MB</td>
                            </tr>
                        </table>
                    </div>
                </div>

                <div class="row mt-4">
                    <div class="col-md-6">
                        <h6>Parent Process</h6>
                        {{if .Process.ParentInfo}}
                        <div class="card">
                            <div class="card-body">
                                <a href="{{$.BasePath}}/sysmon/process/{{.Process.ParentInfo.PID}}">
                                    <strong>{{.Process.ParentInfo.PID}}</strong> - {{.Process.ParentInfo.Name}}
                                </a>
                                <br>
                                <small class="text-muted"><code>{{.Process.ParentInfo.Cmdline}}</code></small>
                            </div>
                        </div>
                        {{else}}
                        <p class="text-muted">No parent process accessible</p>
                        {{end}}
                    </div>
                    <div class="col-md-6">
                        <h6>Child Processes</h6>
                        {{if .Process.ChildrenInfo}}
                        <div class="card">
                            <div class="card-body">
                                <ul class="list-unstyled mb-0">
                                    {{range .Process.ChildrenInfo}}
                                    <li class="mb-2">
                                        <a href="{{$.BasePath}}/sysmon/process/{{.PID}}">
                                            <strong>{{.PID}}</strong> - {{.Name}}
                                        </a>
                                        <br>
                                        <small class="text-muted"><code>{{.Cmdline}}</code></small>
                                    </li>
                                    {{end}}
                                </ul>
                            </div>
                        </div>
                        {{else}}
                        <p class="text-muted">No child processes</p>
                        {{end}}
                    </div>
                </div>
            </div>
        </div>

        <div class="card mt-3">
            <div class="card-body">
                <h6 class="card-title">Send Signal to Process</h6>
                <p class="text-muted small">Send a Unix signal to this process. Use with caution.</p>
                <form hx-post="{{.BasePath}}/sysmon/process/{{.Process.PID}}/hx-signal"
                      hx-target="#signal-result"
                      hx-swap="innerHTML">
                    <div class="row g-2">
                        <div class="col-auto">
                            <select class="form-select form-select-sm" name="signal" required style="min-width: 300px;">
                                <option value="">Select signal...</option>
                                {{range .Signals}}
                                <option value="{{.Number}}">{{.Name}} ({{.Number}}) - {{.Description}}</option>
                                {{end}}
                            </select>
                        </div>
                        <div class="col-auto">
                            <button type="submit" class="btn btn-danger btn-sm">Send Signal</button>
                        </div>
                    </div>
                </form>
                <div id="signal-result" class="mt-3"></div>
            </div>
        </div>
    </div>
</body>

</html>
